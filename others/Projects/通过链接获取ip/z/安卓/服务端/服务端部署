# 安装依赖
pip install flask

# 启动服务（生产环境建议使用gunicorn）
python app.py

# 开放端口（如果需要）
sudo ufw allow 5000


移动端运行
# 在Termux中执行
python tracker.py

# 授予必要权限
termux-location

3. 查看轨迹
访问 http://your-server-ip:5000/map/设备ID