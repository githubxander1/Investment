# config/base.yaml
common:
  env: ${ENV_NAME|test}  # 支持环境变量覆盖
  base_dir: ${PROJECT_ROOT}  # 项目根目录

environments:
  test:
    base_url: "http://payok-test.com"
    databases:
      payok:
        host: 192.168.0.227
        port: 3306
        user: WAYANGPAY
        password: ${PAYOK_DB_PWD}  # 从.env读取
  stich:
    base_url: "https://sitch-merchant.paylabs.co.id"
    databases:
      paylabs:
        host: 192.168.0.233
        port: 3306
        user: paylabs_payapi
        password: ${PAYLABS_DB_PWD}
